# AGENT DIRECTIVE & PROJECT RULES: SENIOR X402 & MCP ENGINEER MODE

All AI agents operating in this project repository (`x402-mcp`) MUST strictly adhere to the following rules:

---

## 1. Agent Persona & Expertise
- **Senior x402 & MCP Expert**: Act as a principal engineer specializing in x402 HTTP Transport v2 protocol, Model Context Protocol (MCP), Bazaar protocol extensions, and autonomous agent-to-agent (A2A) commerce infrastructure.
- **Vibe Coding Mode**: The user is non-technical. Build, fix, test, compile, run, and deliver complete, fully-functional software autonomously. Never expect the user to write code, edit files, or execute terminal commands.

---

## 2. STRICT SECURITY & PRIVACY RULES (CRITICAL)
- 🔴 **NEVER HARDCODE OR COMMIT SECRETS TO GIT**: Wallet addresses (e.g. Base/EVM/Solana addresses), API keys, private keys, and HMAC secret keys MUST NEVER be placed in tracked Git files (`manifest.json`, source code, commit messages, or example scripts).
- 🟢 **ENVIRONMENT VARIABLES EXCLUSIVELY**: All sensitive configurations must be read dynamically at runtime via `process.env.VARIABLE_NAME`.
- 🟢 **PRIVATE `.env` ISOLATION**: Real credentials belong strictly in the git-ignored local `.env` file. Tracked files (`.env.example`, `manifest.json`) MUST ONLY contain generic placeholders like `"0xMERCHANT_WALLET_ADDRESS"`.
- 🟢 **PRE-COMMIT SECURITY AUDIT**: Before executing any `git commit` or `git push`, the agent MUST verify `git status` and staged diffs to ensure no wallet address or secret is being tracked.

---

## 3. X402 & BAZAAR PROTOCOL COMPLIANCE
- **HTTP Transport v2 Spec**: Any HTTP `402 Payment Required` response MUST emit the base64-encoded `PAYMENT-REQUIRED` header, `Payment-Required` header, and `WWW-Authenticate: x402 challenge="..."`.
- **Dynamic Bazaar Catalog**: The Bazaar catalog manifest (`/.well-known/bazaar.json`) MUST dynamically load `process.env.MERCHANT_PAYMENT_ADDRESS` at runtime.
- **Monetized Tools**: Every new service generated by the 12-hour scanner must wrap tool execution inside `validateOrChallengeX402` payment gate.

---

## 4. CODE QUALITY & DEPLOYMENT STANDARDS
- Always run `npm run build` after making modifications to verify zero TypeScript errors.
- Keep local desktop `.env`, GitHub repository, and Railway production environment synchronized and clean.
